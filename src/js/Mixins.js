export const basic_utilities = {
    methods: {
        add: function(a, b){
            return a + b;
        },
        OpenExternalLinkInNewTab: function(link){
            window.open(link, '_blank');
        }
    }
}